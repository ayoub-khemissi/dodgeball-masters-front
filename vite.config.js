import { defineConfig } from 'vite'

export default defineConfig({
    // Base path pour GitHub Pages
    // Correspond au nom de ton dépôt : /dodgeball-masters-front/
    base: '/dodgeball-masters-front/',
})
